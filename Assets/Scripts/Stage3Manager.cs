using UnityEngine;

public class Stage3Manager : MonoBehaviour
{
    public bool kakaIsSaved = false;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
